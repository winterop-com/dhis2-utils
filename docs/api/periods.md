# Periods

Hand-written `PeriodType` StrEnum (24 canonical DHIS2 period types — `Daily`, `Weekly`, `Monthly`, `Quarterly`, etc.). Upstream `PeriodType` is a Java class hierarchy so DHIS2's `/api/schemas` doesn't expose it; we list the names here, sourced from `PeriodTypeEnum.java`.

::: dhis2_client.periods
