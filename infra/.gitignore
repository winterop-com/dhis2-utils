# macOS
.DS_Store

# Database dump
*.sql.gz
# Exception: the tiny e2e test dump IS committed so GitHub Actions can run the
# stack without any external secrets. Keep it small (few MB, 1–2 users + a
# sample dataset) — any real / production dumps under a different filename
# stay ignored by the pattern above.
!dhis.sql.gz

# DHIS2 runtime data
home/logs/
home/files/
home/glowroot/
home/credentials/

# Credentials
.env
home/dhis-google-auth.json

# Notes
notes/

# Python / uv
.venv/
__pycache__/
*.pyc

# Playwright MCP runtime artifacts
.playwright-mcp/

# Verification screenshots taken during sessions
*.png

# Stray uv-init placeholder
main.py
